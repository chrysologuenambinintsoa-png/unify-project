'use client';

import React from 'react';

// Video call button removed — render nothing to avoid showing feature
export function VideoCallButton() {
  return null;
}

export default VideoCallButton;
