/**
 * Use a different distDir to avoid permission issues on the default `.next` folder
 */
module.exports = {
  // Use a different build directory to avoid locked trace files
  distDir: '.next_dev2'
}
